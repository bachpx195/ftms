<%= react_component "SubjectsShowBox",
  props: {
    subject: @subject,
    assigments: @subject_supports.user_assigmnent,
    current_user: current_user,
    course_subject_id: @course_subject ? @course_subject.id : nil,
    user_dynamic_course_subjects: @subject_supports.user_dynamic_course_subjects,
    course: @course}, prerender: false
%>
